[project]
name = "access-sync"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "flask>=3.0.0",
    "discord.py>=2.3.2",
    "python-dotenv>=1.0.0",
]

[tool.vercel]
entrypoint = "dashboard:app"
